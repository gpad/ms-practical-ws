describe("RabbitEventBus", () => {
  it("ack message when return ack")
  it("nack and reenqueue message when handler raise exception")
  it("nack and reenqueue message when handler raise exception")
  it("nack and DONT reenqueue message when message was already reenqueue")
  it("raise exception if handler is equal")
  it("create queue if already started")
  it("when start should have all builder")
})

describe("Rabbit", () => {
  it("ack")
  it("nack")
})
